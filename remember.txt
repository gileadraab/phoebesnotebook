-- terminar de adaptar template sidebar.html (em especial fazer funcionar site.pages e social networks). Ignorar tudo relacionado a rss
-- criar page about (criar nova rota, reutilizar template do portifolio (pensar no conteudo exato))
-- pr para versao alpha com items acima

-- fazer funcionar /mypost. Para isso, criar objeto post(atualmente chamado de pages no post.html - lembrar de fazer find: page and replace: post) com todas as propriedades requeridas pelo template (e pelo script do Lunr). Decidir se quer categories ou tags, remover o que nao quiser.

-- metadata sobre posts deve vir de um yml. Especificamente, quando a url /post/<slug> for acessada vamos procurar por um arquivo <slug>.yml dentro da pasta posts. Esse yml, when parsed deve resultar em um dict com a mesma estrutura da variavel "post" atual
-- ajustar layout do post final. Investigar/comparar entre codeblocks corretos e incorretos. Isso inclui o styling dos fenced codeblocks, entender o que esta acontecendo com as listas (pedir ajuda do alan) e syntax highlighting do código (checar com o alan)

-- na index utilizar url_for passando slug como argumento